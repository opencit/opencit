The Monit installer attempts to install monit packages through the package manager first.
The Monit installer unpacks, compiles, and configures monit for monitoring Mt Wilson services.
Standard linux developer tools gcc, make, etc. are required if you install it from source.
