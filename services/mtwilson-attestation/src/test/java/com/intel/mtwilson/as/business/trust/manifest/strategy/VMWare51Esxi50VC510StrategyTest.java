package com.intel.mtwilson.as.business.trust.manifest.strategy;


import org.junit.Test;


public class VMWare51Esxi50VC510StrategyTest {

    // TODO this should move to integration testing since it calls a different host on  the network
	@Test
	public void test() {
            /*
		try {
			
			
			new VMWare50Esxi50Strategy().getManifest(new TblHostsJpaController(new BaseBO().getEntityManagerFactory()).findByName("10.1.71.141"));
		} catch (Exception e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
			fail("VMWare51Esxi50VC510Strategy");
		}
                * 
                */
	}

}
